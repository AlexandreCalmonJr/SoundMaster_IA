# ===== CORE (obrigatório — servidor não funciona sem estes) =====
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0
numpy>=1.26.0
scipy>=1.12.0
python-multipart>=0.0.9
requests>=2.28.0
tqdm>=4.64.0
sounddevice>=0.5.0  # WASAPI/ASIO capture via Python
# ===== OPCIONAIS (instalados separadamente — podem falhar sem quebrar o servidor) =====
# --- Audio Classification (YAMNet) ---
# Linux: tflite-runtime (leve, ~20MB)
tflite-runtime>=2.14.0; sys_platform == 'linux' and python_version < '3.13'
# Windows/macOS: tensorflow.lite embutido no tensorflow (~500MB)
tensorflow>=2.12.0; sys_platform != 'linux' and python_version < '3.13'
# --- Local LLM (fallback secundário — Ollama é o primário) ---
# Windows: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
